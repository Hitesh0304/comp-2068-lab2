extends ../layouts/main.pug
block content
  .container
    header
      h1= book.title
      small= book.status.toLowerCase()

    div
      = book.content || "Your book is going through tense writing process"